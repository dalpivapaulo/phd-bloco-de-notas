(()=>{const C=window.PHD_CONFIG||{},K="phd_session_v1";let token=localStorage.getItem(K)||"",profile=null,items=[],projectCode="";const $=id=>document.getElementById(id),esc=s=>String(s??"").replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#039;'}[c]));
const okCfg=()=>/^https:\/\//.test(C.SUPABASE_URL||"")&&(C.SUPABASE_ANON_KEY||"").length>20;function msg(t){$("authMessage").textContent=t;$("authMessage").classList.remove("hidden")}function clr(){$("authMessage").classList.add("hidden")}function toast(t){let e=$("toast");e.textContent=t;e.classList.add("show");clearTimeout(toast.t);toast.t=setTimeout(()=>e.classList.remove("show"),2200)}function view(id){["startView","codeForm","registerForm","loginForm"].forEach(x=>$(x).classList.toggle("hidden",x!==id));clr()}
function today(d=new Date()){return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,"0")}-${String(d.getDate()).padStart(2,"0")}`}function fdate(x){if(!x)return"Sem data";let[y,m,d]=x.split("-");return`${d}/${m}/${y}`}function wday(x){if(!x)return"";let[y,m,d]=x.split("-").map(Number);return new Intl.DateTimeFormat("pt-BR",{weekday:"long",timeZone:"UTC"}).format(new Date(Date.UTC(y,m-1,d)))}function bucket(r){if(r.status==="done")return"done";if(!r.due_date)return"future";if(r.due_date<today())return"late";if(r.due_date===today())return"today";return"future"}function size(t,p=false){let n=(t||"").length;if(p){if(n>160)return"5.6pt";if(n>110)return"6.2pt";if(n>70)return"6.8pt";return"7.6pt"}if(n>260)return"12px";if(n>180)return"13px";if(n>110)return"14px";return"15px"}
async function rpc(fn,b={}){if(!okCfg())throw Error("Configuração do Supabase pendente.");let r=await fetch(`${C.SUPABASE_URL}/rest/v1/rpc/${fn}`,{method:"POST",headers:{apikey:C.SUPABASE_ANON_KEY,"Content-Type":"application/json"},body:JSON.stringify(b)}),tx=await r.text(),d;try{d=tx?JSON.parse(tx):null}catch{d=tx}if(!r.ok)throw Error(d?.message||d?.hint||d?.details||`Erro ${r.status}`);return d}
async function boot(){let p=await rpc("current_profile",{p_token:token});profile=Array.isArray(p)?p[0]:p;if(!profile?.id)throw Error("Sessão inválida");items=await rpc("list_reminders",{p_token:token})||[];$("auth").classList.add("hidden");$("workspace").classList.remove("hidden");$("welcome").textContent=`Olá, ${profile.name}`;$("todayLabel").textContent=new Intl.DateTimeFormat("pt-BR",{dateStyle:"full"}).format(new Date());render()}
function sort(a){let p={Alta:0,Média:1,Baixa:2};return a.sort((x,y)=>(x.due_date||"9999").localeCompare(y.due_date||"9999")||(x.due_time||"99:99").localeCompare(y.due_time||"99:99")-0+(p[x.priority]-p[y.priority]))}function note(r){return`<article class="note priority-${esc(r.priority)}" data-id="${esc(r.id)}"><div class="date">${esc(fdate(r.due_date))}</div><div class="weekday">${esc(wday(r.due_date))}</div><div class="time">${esc(r.due_time?r.due_time.slice(0,5):"—")}</div><div class="text" style="font-size:${size(r.text)}">${esc(r.text)}</div></article>`}function fill(id,a){$(id).innerHTML=a.length?a.map(note).join(""):'<div class="empty">Nenhum lembrete.</div>'}
function render(){let g={late:[],today:[],future:[],done:[]};items.forEach(r=>g[bucket(r)].push(r));Object.values(g).forEach(sort);fill("lateGrid",g.late);fill("todayGrid",g.today);fill("futureGrid",g.future);fill("doneGrid",g.done);$("lateSection").classList.toggle("hidden",!g.late.length);[["lateCount","mLate",g.late.length],["todayCount","mToday",g.today.length],["futureCount","mFuture",g.future.length],["doneCount","mDone",g.done.length]].forEach(([a,b,n])=>{$(a).textContent=n;$(b).textContent=n});document.querySelectorAll(".note").forEach(e=>e.addEventListener("click",()=>edit(e.dataset.id)));buildPrint(g)}
function openNew(){$("dialogTitle").textContent="Novo lembrete";$("remId").value="";$("remText").value="";$("remDate").value=today();$("remTime").value="";$("remPriority").value="Média";$("deleteBtn").classList.add("hidden");$("doneBtn").classList.add("hidden");$("dialog").showModal()}function edit(id){let r=items.find(x=>x.id===id);if(!r)return;$("dialogTitle").textContent="Editar lembrete";$("remId").value=r.id;$("remText").value=r.text;$("remDate").value=r.due_date||"";$("remTime").value=r.due_time?r.due_time.slice(0,5):"";$("remPriority").value=r.priority;$("deleteBtn").classList.remove("hidden");$("doneBtn").classList.remove("hidden");$("doneBtn").textContent=r.status==="done"?"Reabrir":"Concluir";$("dialog").showModal()}
async function save(){let t=$("remText").value.trim();if(!t){toast("Digite o lembrete.");return}await rpc("save_reminder",{p_token:token,p_id:$("remId").value||null,p_text:t,p_due_date:$("remDate").value||null,p_due_time:$("remTime").value||null,p_priority:$("remPriority").value});$("dialog").close();items=await rpc("list_reminders",{p_token:token})||[];render();toast("Lembrete salvo.")}async function del(){let id=$("remId").value;if(!id||!confirm("Excluir este lembrete?"))return;await rpc("delete_reminder",{p_token:token,p_id:id});$("dialog").close();items=await rpc("list_reminders",{p_token:token})||[];render()}async function toggle(){let id=$("remId").value;if(!id)return;await rpc("toggle_reminder",{p_token:token,p_id:id});$("dialog").close();items=await rpc("list_reminders",{p_token:token})||[];render()}
function pnote(r){return`<div class="print-note"><div class="print-date">${esc(fdate(r.due_date))}</div><div class="print-weekday">${esc(wday(r.due_date))}</div><div class="print-time">${esc(r.due_time?r.due_time.slice(0,5):"—")}</div><div class="print-text" style="font-size:${size(r.text,true)}">${esc(r.text)}</div></div>`}function psec(t,a,l){a=a.slice(0,l);return a.length?`<section class="print-section"><h3>${t}</h3><div class="print-grid">${a.map(pnote).join("")}</div></section>`:""}function buildPrint(g){let ll=Math.min(10,g.late.length),tl=Math.min(10,g.today.length),fl=Math.min(g.late.length?10:20,g.future.length),h=`<div class="print-page"><div class="print-title">PHD | Bloco de Notas</div>${psec("ATRASADOS",g.late,ll)}${psec("PARA HOJE",g.today,tl)}${psec("FUTUROS",g.future,fl)}</div>`,r=[...g.late.slice(ll),...g.today.slice(tl),...g.future.slice(fl)];for(let i=0;i<r.length;i+=30)h+=`<div class="print-page"><div class="print-title">PHD | Bloco de Notas</div><section class="print-section"><h3>CONTINUAÇÃO</h3><div class="print-grid">${r.slice(i,i+30).map(pnote).join("")}</div></section></div>`;$("printStage").innerHTML=h}
async function logout(){try{if(token)await rpc("logout_user",{p_token:token})}catch{}localStorage.removeItem(K);token="";$("workspace").classList.add("hidden");$("auth").classList.remove("hidden");view("startView")}
$("newUserPath").onclick=()=>view("codeForm");$("userPath").onclick=()=>view("loginForm");document.querySelectorAll(".back").forEach(b=>b.onclick=()=>view("startView"));$("codeForm").onsubmit=async e=>{e.preventDefault();clr();try{let c=$("projectCode").value.trim();if(await rpc("validate_project_code",{p_code:c})===true){projectCode=c;view("registerForm")}else msg("Código do projeto inválido.")}catch(er){msg(er.message)}};$("registerForm").onsubmit=async e=>{e.preventDefault();clr();let p=$("regPin").value;if(p!==$("regPin2").value){msg("Os PINs não conferem.");return}try{let d=await rpc("register_user",{p_project_code:projectCode,p_name:$("regName").value.trim(),p_username:$("regUser").value.trim().toLowerCase(),p_pin:p}),r=Array.isArray(d)?d[0]:d;if(!r?.success)throw Error(r?.message||"Não foi possível criar o usuário.");token=r.session_token;localStorage.setItem(K,token);projectCode="";await boot()}catch(er){msg(er.message)}};$("loginForm").onsubmit=async e=>{e.preventDefault();clr();try{let d=await rpc("login_user",{p_username:$("loginUser").value.trim().toLowerCase(),p_pin:$("loginPin").value}),r=Array.isArray(d)?d[0]:d;if(!r?.success)throw Error(r?.message||"Login inválido.");token=r.session_token;localStorage.setItem(K,token);await boot()}catch(er){msg(er.message)}};
$("newBtn").onclick=openNew;$("printBtn").onclick=()=>window.print();$("logoutBtn").onclick=logout;$("closeDialog").onclick=()=>$("dialog").close();$("cancelBtn").onclick=()=>$("dialog").close();$("remForm").onsubmit=async e=>{e.preventDefault();try{await save()}catch(er){toast(er.message)}};$("deleteBtn").onclick=async()=>{try{await del()}catch(er){toast(er.message)}};$("doneBtn").onclick=async()=>{try{await toggle()}catch(er){toast(er.message)}};if("serviceWorker"in navigator)window.addEventListener("load",()=>navigator.serviceWorker.register("./service-worker.js").catch(()=>{}));(async()=>{if(!okCfg()){msg("Próxima etapa: configurar o Supabase no arquivo config.js.");return}if(token)try{await boot();return}catch{localStorage.removeItem(K);token=""}view("startView")})()})();
